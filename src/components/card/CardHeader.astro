---
import clsx from "clsx";
const { class: className } = Astro.props;
---

<div class="card--header flex flex-row w-auto items-center justify-between">
    <h2
        class={clsx(
            "font-medium text-primary-900 mb-0 md:text-2xl text-lg",
            className
        )}
    >
        <slot />
    </h2>
</div>
